Features
	towns on islands
	islands with dungeons / warps
	ECS, compress item data, and future entity type data
		stats
		inventory
		alliance
	post collision checks / behaviour
	make spawning new entities an action
	make throwing items an action

Optimizations / Improvements
	better data structures: 
		dictionary (sucks, adding entries is very slow: should probably use some other internal structure, hash map, tree, etc.)
		priority queue (really sucks, use heap or something)
		graph (deleting edges causes unexpected null returns, this feels like laziness)
	put debug code in ifdef's -- maybe done
	do better string stuff, current string stuff is probably bad / dangerous
	ray casting collision
	have generic entity create, all data stored in cfg file
	split tile data between flags and id's, should improve draw loop
	in delaunay triangulation, store edges in counter-clockwise order

Bugs
	NPC sword swings / actions causing memory leaks
	NPC's need better way to target player, segfault on player death / current implementation is prone to crash 
	fix delaunay get_candidate function to work with extreme angles that currently cause it to fail from floating point imprecision

Non-specific improvements / features
	further island improvements / more interesting islands
	better dungeons
	better combat
	more enemies
	more items